<div class="box box-info">
  <div class="box-header with-border">
    <h3 class="box-title">Category Details</h3>
  </div>
  <div class='form-horizontal category'>
    <div class="box-body">
      <div class="col-md-6 col-xs-offset-2">
        <div class="form-group">
          <label class='col-sm-3 control-label'> Title </label>
          <div class="col-sm-9">
            <div class='form-control'><%= @category.title %></div>
          </div>
        </div>
      </div>
    </div>

    <div class=" box-footer">
      <div class="col-md-6 col-xs-offset-2 pull-right">
        <%= link_to t('.edit', :default => t("helpers.links.edit")), edit_category_path(@category), :class => 'btn btn-info' %>
        <%= link_to t('.delete', :default => t("helpers.links.delete")), @category, method: :delete, :class => 'btn btn-danger', confirm: 'Are you sure?' %>
        <%= link_to t('.back', :default => t("helpers.links.back")), categories_path, :class => 'btn btn-default' %>
      </div>
    </div>

  </div>
</div>